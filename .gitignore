/target
**/*.rs.bk
Cargo.lock.orig

# Zed extension build artifacts
/extension/target
*.wasm

# Local-only Claude Code settings (kept on disk, never tracked)
.claude/

# Editor / OS noise
.DS_Store
Thumbs.db
.idea/
*.swp

# PyPilot runtime state that must never be committed
/helper/.pypilot-cache/
